<%= link_to  '#offcanvasFilter', class: 'btn btn-outline-primary ms-1 me-1 position-relative', data: { bs_toggle: 'offcanvas', query: params[:q]} do %>
    <%= tag.span slider_vertical_icon %>
    <span class="<%= informer_class%> <% if !params[:q]&.values&.reject(&:blank?).present? %>d-none<% end %>" id = "filter-informer">
    <span class="visually-hidden"></span>
    </span>
<% end %>
