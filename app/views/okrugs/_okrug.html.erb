<tr id="<%= dom_id okrug %>">
    <td><b><%= okrug.id%></b></td>
    <td><%= okrug.title %></td>
    <td>
        <%= link_to '<i data-feather="edit-2"></i>'.html_safe, edit_okrug_path(okrug), class: 'px-2' %>
        <%= link_to '<i data-feather="trash-2"></i>'.html_safe, okrug, data: { "turbo-method": :delete, confirm: 'Are you sure?' } %>
    </td>
</tr>