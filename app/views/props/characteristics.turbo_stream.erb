
<%= turbo_stream.replace @wraptarget do %>
  <div class="mb-3 col-md-5" data-prop-target='characteristicwrap' id='<%= @wraptarget%>' >
      <%= select_tag @targetname, options_for_select(@characteristics), 
                                data: { controller: "slimselect", 
                                prop_target: "select",
                                search_prop_char_url:  "/properties/#{@property.id}/characteristics/search" } %>
  </div>
<% end %>
