
<tr id="<%= dom_id warehouse %>">
    <td><b><%= warehouse.id%></b></td>
    <td><%= warehouse.title %></td>
    <td>
    <%= link_to '<i data-feather="edit-2"></i>'.html_safe, edit_warehouse_path(warehouse), class: 'px-2' %>
    <%= link_to '<i data-feather="trash-2"></i>'.html_safe, warehouse, data: { "turbo-method": :delete, confirm: 'Are you sure?' }, class:"px-2" %></td>
</tr>