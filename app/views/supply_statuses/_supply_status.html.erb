<%= turbo_frame_tag supply_status, class: "d-flex list-group-item py-2 border-start-0 border-end-0 row", data: {
    sort_url: sort_supply_status_path(supply_status.id), sort_item_id: supply_status.id }  do %>
  <%= tag.div(class: "col-1") do %>
    <%= sortable_with_badge(supply_status.position)%>
    <%= supply_status.id%>
  <% end %>
    <%= tag.div(supply_status.title, class: "col-5")%>
    <%= tag.div('', class: "col-4") do %>
        <%= tag.span('', class: 'spinner-grow-static', style: "background-color: #{supply_status.color};")%>
    <% end %>
    <div class= "col-2 d-flex justify-content-end gap-2 align-items-center">
        <%= render 'shared/edit_delete_link_turbo', object: supply_status %>
    </div>
<% end %>