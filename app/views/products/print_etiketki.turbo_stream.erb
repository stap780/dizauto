<%= turbo_stream.update "modal", template: "products/pending_etiketki", layout: false if @pr_size >= 10 %>
<%= turbo_stream.update("modal", template: "products/success_etiketki", layout: false, locals: {etiketka: @etiketka}) if @pr_size < 10 %>
<%= turbo_stream_action_tag :set_unchecked, targets: '.checkboxes' %>
<%= turbo_stream_action_tag :open_modal, targets: '.modal' %>
