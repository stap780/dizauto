<!DOCTYPE html >
<html>
	<meta charset="utf-8"/>
	<head>
		<title>Деталь - <%= variant.product.title.mb_chars.capitalize.to_s %></title>
		<style>
			body {font-size: 12px;}
			.content { margin: 0 auto; text-align: center;}
			/* Стили штрихкода*/
			table.barby-barcode { border-spacing: 0; }
			tr.barby-row {}
			td.barby-cell { height: 60px; }
			td.barby-cell.on { background: #000; }
			/*  */
		</style>
	</head>
	<body>
		<div class="content">
			<div style="font-size: 14px;"><%= variant.product.title.mb_chars.capitalize.to_s.at(0..26) if variant.product.title != nil %></div>
			<div style="margin: 1px 5px; ">
				<%= variant.html_barcode  %>
			</div>
			<div>
				<%# product.markatext %> <%# @item.modeltext %>
				<span style="font-weight: bold;font-size: 28px;"><%= variant.barcode.to_s %></span>
			</div>  
			<div>
				Ор.Н. <%= variant.sku %>
			</div>
		</div>
	</body>
</html>