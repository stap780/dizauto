<%#  не использую - старый вариант модального окна %>

<%= render '/shared/modal', title: 'Import file' do %>
    <%= form_tag import_setup_incases_path, multipart: true, data: { turbo: false } do %>
        <div class="mt-2 mb-2"><b>ВНИМАНИЕ</b> для импорта подходят файлы (Excel 2000/97 .xls) или csv (разделитель "запятая", кодировка "utf-8")</div>
            <p class="mt-5"></p>
            <%= file_field_tag :file, :class =>'import_file'%>
            <p class="mt-5"></p>
            <p>Что нужно сделать при импорте:</p>
            <p>- проверяем есть ли страховая. если нет, то выводим предупреждение и просим создать страховую</p>
            <p>- проверяем есть ли компания. если нет, то выводим сообщение что нет и просим создать компанию</p>
            <p>- проверяем есть ли такой убыток. если да, то выводим сообщение что он есть и выводим данные по убытку с позициями.</p>
            <p>- после сохранения убытка нужно отправить письмо каждому контрагенту (['Контрагент','Страховая компания', 'Номер З/Н СТОА', 'Номер дела', 'Марка и Модель ТС', 'Гос номер', 'Деталь'])</p>
            <p class="mb-5"></p>	
            <%= submit_tag "Импорт", class: "btn btn-primary" %>
        </div>
        <div class="modal-footer">
        </div>
    <% end %>
<% end %>