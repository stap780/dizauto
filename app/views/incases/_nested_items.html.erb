<tr id="<%= dom_id f.object %>" class="nested-form-wrapper<%= nested_form_index.to_s%>" data-new-record="<%= f.object.new_record? %>">
  <%= render "incase_items/form_data", f: f, import_line: nil %>
  <%= td_remove_nested(f)%>
</tr>
